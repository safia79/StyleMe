# FR: Foundation — project setup (no feature logic yet)
# Copy this file to .env and adjust values if needed.

DATABASE_URL="postgresql://postgres.<project-ref>:<password>@<host>:5432/postgres?sslmode=require&connect_timeout=30"
SESSION_SECRET="styleme-dev-secret-change-me"
PORT=3001
FRONTEND_URL="http://localhost:5173"
# Secret key from https://dashboard.stripe.com/apikeys (starts with sk_test_)
# Do not use the placeholder — Stripe will reject it and checkout will fail.
STRIPE_SECRET_KEY="sk_test_your_key_here"
