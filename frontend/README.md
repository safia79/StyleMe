# StyleME frontend

FR: Foundation — project setup (no feature logic yet)

This is the React (Vite) app for StyleME.

Install and run steps are in the root `README.md` (project folder, one level up).
